{
  "name": "weeklybot",
  "version": "1.0.0",
  "main": "dist/weeklybot.js",
  "author": "Mobsuccess",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prestart": "npm run build",
    "start": "node dist/weeklybot.js",
    "prebuild": "rm -rf dist",
    "build": "tsc --build"
  },
  "dependencies": {
    "@slack/bolt": "^3.12.1",
    "@slack/web-api": "^6.7.2",
    "dotenv": "^16.0.2"
  }
}